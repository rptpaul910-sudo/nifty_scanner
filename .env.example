# MySQL Configuration
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=your-mysql-password
MYSQL_DB=nifty50_scanner

# Flask Configuration
SECRET_KEY=replace-with-a-long-random-string
DEBUG=True

# Note: Angel One credentials no longer needed.
# Market data is now fetched via Yahoo Finance (free, no auth required).
